import { LoadShowDetailDirective } from './load-show-detail.directive';

describe('LoadShowDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadShowDetailDirective();
    expect(directive).toBeTruthy();
  });
});
